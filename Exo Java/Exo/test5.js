let VariablesGlobale = 10;

for ( let index =0; index < VariablesGlobale; index ++)  {
    const vallectureSeule = index *2;
}

function multiplier (n1, n2) {
    let resultat = n1 * n2;

    if (resultat > VariablesGlobale) {
      let sup = resultat;
    }
    return sup; //Impossible car hors de portée
}